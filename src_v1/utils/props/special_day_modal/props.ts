import { string} from '../../../Resources';
const props = {
  title: string.special_modal_title,
  desc: string.special_day_modal_desc,
  footer: string.special_day_modal_footer,
};
export default props;
